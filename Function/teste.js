const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);

const MUITO_GRANDE_MESMO = `// ${"█".repeat(200)}\n`.repeat(12000);


const GIGACHAD_BYTES = "tá bom, para de encher o arquivo aí ".repeat(250_000);
